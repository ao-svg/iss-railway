{
  "name": "iss-fixture-pipeline",
  "version": "1.0.0",
  "description": "Fetches soccer fixtures from TheSportsDB + matches TV channels against the public iptv-org playlist, outputs CSV/JSON.",
  "main": "src/index.js",
  "type": "commonjs",
  "scripts": {
    "start": "node src/index.js",
    "run-once": "node src/index.js --once",
    "test": "node --test"
  },
  "dependencies": {
    "axios": "^1.7.7",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "node-cron": "^3.0.3"
  },
  "engines": {
    "node": ">=18"
  }
}
